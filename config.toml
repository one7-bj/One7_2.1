# ============================================================
# ONE7 V2.1
# config.py
# ============================================================

import streamlit as st


def get_supabase_url() -> str:
    """Retourne l'URL Supabase configurée."""
    try:
        return st.secrets["SUPABASE_URL"]
    except Exception:
        raise RuntimeError(
            "SUPABASE_URL est introuvable dans .streamlit/secrets.toml"
        )


def get_supabase_key() -> str:
    """Retourne la clé publique Supabase."""
    try:
        return st.secrets["SUPABASE_KEY"]
    except Exception:
        raise RuntimeError(
            "SUPABASE_KEY est introuvable dans .streamlit/secrets.toml"
        )


APP_NAME = "One7"
APP_VERSION = "2.1.0"

CURRENCY = "XOF"
CURRENCY_LABEL = "FCFA"
